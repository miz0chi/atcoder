#include <bits/stdc++.h>

using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int h, w;
  cin >> h >> w;

  for (int i = 0; i < h; ++i) {
    string s;
    cin >> s;
    cout << s << '\n';
    cout << s << '\n';
  }

  return 0;
}
